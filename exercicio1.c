#include<stdio.h>
#include<math.h>

main() {

    //Criar um algoritmo que receba o valor de x, e calcule e imprima o valor de f(x).

    float x, fx; 

    printf("Digite o valor de x: ");
    scanf("%f", &x);

    if (fx = (5 * x + 3) / (x * x - 16)) {

        printf("O valor de f(x) é: %.2f\n", fx);
    } else {
        printf("A função não está definida para x = 4 ou x = -4 (denominador igual a zero).\n");
    }

}
